# SDD ledger — plan: docs/superpowers/plans/2026-08-30-runtime-first-roadmap.md

Task 8 completed at 0dc05f2. User reprioritized the program on 2026-08-30: P0 Runtime Federation, P1 five architecture-level reliability updates, P2 original Agent product features.

Task RF-1.0: started after design review identified runtime assignment/instance/gate-proof contracts as mandatory prerequisites to routing.
Review cadence ruling: task rounds cover specification, correctness, recovery consistency and code quality. Broad security review and fault injection are deferred to one dedicated final phase after P0, P1 and P2 are complete; existing correctness findings remain in scope.
Task RF-1.0: fix round 1/5 closed injectable production trust, proof timing/domain, direct takeover and lease integrity gaps; durable recovery evidence and state-machine gaps remained (commits 3a78d3a..189dd96).
Task RF-1.0: fix round 2/5 made acceptance/effect evidence durable and recovery outcomes idempotent; write-first Effect lattice and recovery source binding remained (commits 189dd96..55ab4eb).
Task RF-1.0: fix round 3/5 closed write-first Effect and source-bound recovery records; full retry-lease decoding remained (commits 55ab4eb..f59f040).
Task RF-1.0: fix round 4/5 used a fresh implementer and formal lease decoding for retry recovery; independent final review CLEAN (commits f59f040..d845b82).
Task RF-2A: pinned Goose and DeepSeek Harness submodules at approved revisions (commit b1d0659).
Task RF-2A: Goose and DeepSeek source-readiness verifiers completed in one shared-index integration commit (commit 4fa31d0); both implementations independently reported complete source-only gates and no Runtime GO claim.
Task RF-2A: fix round 1/5 closed reproducible Goose and per-target DeepSeek build-plan findings; independent review CLEAN and shared Python Term manifest refreshed (commits 1eae3e3, a729735, 08eb4f1).
Task RF-1.1: runtime-neutral selector, durable admission intent and immutable RuntimeAssignment integration implemented. Fix round 1/5 enabled the real build_app path, added cross-process command identity arbitration, made terminal CAS idempotent and detached pending recovery from mutable catalog state. Fix round 2/5 preserved pre-v28 accepted-command recovery without claim pollution. Independent review CLEAN and controller verification complete (commit f9fd0ff).
Task RF-1.2: DEV_UNTRUSTED Python user acceptance path implemented: request-time admission diagnostics, command read model, atomic development proof preparation, fixed read-only virtual Workspace, explicit Runtime selector, Electron IPC/environment wiring and self-contained real E2E. Fix round 1/5 closed frozen-grant recovery, Workspace parent-symlink escape and fixture-only E2E. Fix round 2/5 made completed/failed terminal idempotency replay stable without repeated Provider or Tool effects. Fix round 3/5 added authorized cleanup of only the current failed prepare temp directory. Fix round 4/5 used a fresh implementer and platform-native atomic no-clobber publish semantics for macOS/Linux. Independent final review CLEAN; controller gates: 1439 Python tests, 36 Goose/DeepSeek source tests and 50 Electron/Playwright tests passed.
Task RF-2B: started. Binding design: one supervised pre-warmed Host v2 process per configured runtime, one exclusive durable fenced lease per query, transient Registry withdrawal before restart, new generation after every crash/terminal, no accepted-command fallback, and no Provider Grant work before RF-2C.
Preflight interface scan:

| Producer | Consumer | Finding | Ruling |
|---|---|---|---|
| `RuntimeProcessConfig` | Sidecar Supervisor | Config is parsed but not consumed; build identity is absent by design. | Trust handshake runtime/build only; validate configured runtime ID equality. |
| `EngineHostV2Client` | Sidecar Supervisor | Client already owns spawn/NDJSON/cancel/process-tree cleanup. | Supervisor composes clients and never duplicates process primitives. |
| `RuntimeRegistryV2` | API/admission | Crash leaves in-memory advertised capabilities ready. | Add transient `withdraw`; reserve `disable` for persistent operator intent. |
| `AssignmentRepository` | Supervised lease handle | Durable fencing exists but has no production caller. | Supervisor owns acquire/renew/transition; evidence uses one narrow internal observer seam. |
| Sidecar Supervisor | `main` / App lifespan | v2 has no lifecycle and Worker order matters. | Start Supervisor before Workers; stop Workers before Supervisor. |
| Sidecar Supervisor | Goose/DSH adapters | Both need the same client lease and generation semantics. | RF-2B exposes one language-neutral handle; adapters cannot receive raw client/process. |
| RF-2B | RF-2C | Provider Grant needs sidecar IPC but secrets are out of scope now. | Preserve a grant seam only; no grant payload or secret handling in RF-2B. |
Task RF-2B brief review: 5 Critical, 6 Important and 2 Minor findings identified before implementation; all were resolved in the brief before dispatch.
Ruling: accepted Query with durable acceptance and no Tool-start follows the approved federation design and may retry on the same fixed Runtime; write-start is persisted before execution, and any unmatched/unknown write forces reconciliation — cost if wrong: an unsafe write replay or an unnecessarily blocked retry.
Ruling: immediate process failure gets a new fenced `recover_failed_lease` path while application restart without proof of old-process death waits for lease expiry — cost if wrong: delayed recovery up to the lease deadline, chosen to avoid concurrent old/new owners.
Ruling: Effect evidence becomes append-only per cursor while the existing aggregate row remains a compatibility projection — cost if wrong: schema and recovery complexity increase, but multi-Tool Queries remain representable.
Ruling: manual `disabled` persists across live re-registration; crash uses transient withdraw under Supervisor generation CAS — cost if wrong: an operator-disabled Runtime could otherwise silently reactivate, or a stale callback could withdraw a fresh process.
Task RF-2B brief review round 2: 3 Critical and 4 Important findings; all resolved before implementation.
Ruling: callers can create only attempt 0; every later attempt is atomically created by a one-time durable retry outcome at exactly source attempt + 1 — cost if wrong: a caller could bypass recovery policy or replay an outcome.
Ruling: a portable process guard plus private containment lock proves prior sidecar termination; cleanup-unconfirmed blocks restart and retry delivery — cost if wrong: availability is sacrificed when containment cannot be proven, chosen to prevent concurrent old/new sidecars.
Ruling: Effect evidence identity includes run/term/step/cursor, not cursor alone — cost if wrong: multi-Step Queries would collide or evidence could be attached to the wrong Step.
Ruling: crash restart budget is cumulative per Supervisor lifecycle and never reset by a handshake; clean recycle is excluded — cost if wrong: a crash-after-handshake loop could restart forever or normal Query turnover could exhaust the budget.
Task RF-2B: initial Supervisor implementation completed at commit 73f765f; focused/backend/source/Electron gates passed, but independent review found recovery, shutdown, fencing and evidence edge cases, so GO was withheld.
Task RF-2B: fix round 1/5 hardened observer-failure reconciliation, one-time recovery consumption, active close/shutdown, restart/fatal handling, build/capability drift, Effect identity reuse, stream cancellation and containment paths (commit fd2e73c).
Task RF-2B: fix round 2/5 corrected durable retirement ordering, shutdown truthfulness, replacement exhaustion finalization, global fatal rollback and promoted evidence replay (commit f43fe98).
Task RF-2B: fix round 3/5 separated outward freeze from durable evidence observation, added late-write reconciliation, terminal/replacement fatal boundaries and promotion provenance (commit e3ab8f7).
Task RF-2B: fix round 4/5 restored approved no-Tool/read-only retry after confirmed cleanup, isolated terminal cleanup failures, completed failed/expired conflict takeover and committed-write replay idempotence (commit b06a74a).
Task RF-2B: fix round 5/5 made recovery-conflict settlement account for an immediate active successor and aligned public active state with durable leases (commit 1f61506).
Task RF-2B: BLOCKED after fix round 5/5. Final independent review reproduced a chained-successor orphan bug: `_orphan_watchdog` tracks the original lease ID, so an external recovery that replaces attempt N with an unexpired attempt N+1 can be mistaken for no active orphan; the local Supervisor then starts/registers a new generation while the durable successor is still active. `GO_SIDECAR_SUPERVISOR` is not granted. Required next action is a separately briefed corrective task that makes the watchdog follow the Runtime's current complete active-lease set (0=start, 1=track current successor, >1=fail closed) and proves attempt0→attempt1→attempt2 takeover without dual advertisement.
