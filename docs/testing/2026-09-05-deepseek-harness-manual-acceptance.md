# DeepSeek Harness 云端人工验收

状态：**真实云端模型链人工验收通过**。2026-09-05 用户从界面发起的验收已取得独立签名证据，并经正式准入加载器复核。该结果只覆盖 DSH 模型执行链，不计为 `GO_DSH_PLUGIN_SMOKE` 或 `GO_RUNTIME_FEDERATION`；后续云端调用仍由用户人工触发，不自动重试验收。

## 输入

### 2026-09-06 流式正文与异常后恢复修复

- 已复现公共文本策略把 `/`、`国资委/集团`、`项目总监 / 业务架构师` 误判为本地路径。修复 Unicode 词边界及独立斜杠；为 `/集团` 这类单段中文流片段增加有限词法例外，覆盖 `国资委/集团` 的全部两段切点。带标签、文件扩展名及多级绝对路径继续拒绝。这不是完整的跨片段敏感内容扫描，不能据此宣称所有自然语言/路径歧义均已解决。
- 完整回答与流片段使用不同长度上限：片段仍为 4,096 字符，完整回答为 65,536 字符，避免普通多角色长报告在汇总事件处失败。其余公共字段验证保留。
- API → Executor → Coordinator → Lease → Supervisor → Client 显式关闭委托异步迭代器，不再等待 GC。正常 `GeneratorExit`、消费取消和锁等待取消不再误升为 Supervisor 全局故障；基础设施故障及无法确认的进程清理仍按既有失败策略处理。
- 新回归先 RED 后 GREEN：真实 Mapper 拒绝事件后立即释放资源、相同命令不重放、下一命令能完成；真实本地协议子进程验证四种消费退出路径后新 Assignment 可执行。合并回归 **1,419 passed / 3 skipped**，独立代码审核无阻塞项。这些不是 DeepSeek 云端验收，跳过项也不计通过。
- 原业务命令的具体丢弃片段未被记录，不能倒推出它一定是 `/`；上面记录的是已稳定复现并修复的同类故障。
- 保留全部历史记录及 **41 条活动暂停记录**。部署后使用新会话重测原业务需求及连续追问，不自动恢复历史探针、不回退聊天模式、不修改或续签过期准入证据。
- 部署状态：补丁 `93d3b60` 已推送当前分支，并重启 Electron/控制面加载；DSH 注册 `ready`、41 条暂停记录不变。已打开模型供应商的 Vault 解锁入口，等待用户在界面解锁后进行新的云端业务会话及追问复测；未发出本轮新云端请求，不宣称用户端端到端已通过。

| 输入 | 当前值 / 取得方式 |
|---|---|
| Runtime | `dsh`，使用正式模型 Host 入口 |
| Provider Profile | `deepseek-primary`，已保存于用户 Workbench 数据库 |
| 模型 | 该 Profile 的 `default` 别名；界面显示保存值，后台读取并冻结实际配置 |
| API 地址和 API Key | 从已保存的 Profile 和加密 Vault 读取；不在聊天中传入 Key |
| Vault 解锁密码 | 在客户端密码框隐藏输入；不是 DeepSeek API Key |
| 网络 | 可以访问该 Profile 配置的 DeepSeek 云端 API；实际请求可能产生费用，底层恢复或供应商重试可能不止一次调用 |

如果需要更换 API Key，先在客户端的模型供应商设置中更新并保存；不要把 Key 或 Vault 密码发送到聊天中。

## 客户端录入（替代命令行）

入口：顶部 **模型供应商 → 解锁保险库 → DeepSeek Harness 人工验收**。

1. 使用已有主密码解锁保险库。需要更新 API Key 时，使用供应商表单中的 API 密钥框保存。
2. 选择已保存且启用的 DeepSeek 供应商，确认显示的默认模型。换模型需先修改并保存供应商配置。
3. 在人工验证区输入与上方解锁相同的保险库密码。外部验证进程只读解密当前凭据快照，不获取主程序的写锁，不需要先锁定主程序。
4. 点击 **开始真实验收** 才会发起真实模型请求；密码提交后立即从表单清空。无需打开终端。
5. 查看运行状态和结果；不想继续时点击 **取消验收**。单次最多运行五分钟，超时不会记为成功。

**测试连接**只检查供应商接口；**开始真实验收**才验证 Harness 执行链。两者不能替代。

这是一次人工发起的实际云端验证，经过固定 DeepSeek Harness 模型 Host、正式准入、Vault/私有 Grant 和事件执行链；不会用 fixture 响应替代真实失败。当前只验收模型请求，不能据此宣布工具、Skill、Workspace 或整个联邦引擎全部通过。

## 结果判定

- 成功：界面显示 `succeeded`；后台外部验证退出码为 `0`，独立目录中生成签名清单和 `runtime-live-evidence-dsh.json`，证据绑定 `dsh`、当前模型/构建、`endpoint_kind=cloud` 和 `terminal=completed`。
- 失败：界面显示 `failed`；保留输出目录，按 Profile、Vault 解锁、网络、模型兼容性和当前构建排查，不把失败记成通过。
- 超时或取消：显示 `timed_out` 或 `cancelled`，不授予验收通过。
- 未运行或未完成：维持 `MANUAL_PENDING / NOT_RUN`。

界面只触发生成独立验证证据，不覆盖原有会话，也不会自动把证据部署到正在运行的客户端。要让会话下拉实际开放 DSH，还需要应用加载对应证据、配置正式模型 Host 并重新检查准入状态；该步骤与前端手测环境接续。证据有有效期，过期或源码/构建变化后需要重新人工验证。

开发者不自动触发 DeepSeek 云端调用，真实结果以用户点击后的本次验证为准。Python Term 与 Goose 的真实 LM Studio 验收结果另见 Task 3 fix2 报告，不能替代本项。

## 同密码验收失败的修复与诊断

旧实现中，客户端解锁后持有跨进程独占写锁，外部验收又尝试以可写模式解锁同一保险库，导致在模型调用前失败。这是进程访问冲突，不是另一套密码。修复后外部验收只读解密，主程序的写入锁机制不变。

失败结果现在展示白名单诊断代码：

- `vault_unlock_failed`：保险库密码校验失败。
- `vault_in_use`：保险库进程锁冲突，不代表密码错误。
- `provider_request_failed`：模型 API 请求失败。
- `runtime_build_unavailable`：本地运行时构建不可用。
- `runtime_verification_failed`：执行或验收证据校验未通过。
- `verification_process_failed`：外部验收进程未正常完成。

旧版本的历史失败没有保存这些分类，不能事后凭通用提示判断密码是否正确。未知错误不再统一建议修改密码；请保留验收编号与诊断代码。

### 2026-09-05 用户复测记录

验收编号 `1cc1ccc942ac44a0b651357a94deda87`，供应商 `deepseek-primary`，保存模型 `deepseek-v4-flash-vision-exp`，结果 `runtime_verification_failed`。独立执行数据库记录 DSH 注册为 ready、准入通过、四次 Grant 已消费，以及四次执行租约释放；未生成最终签名验收证据。因此本次不是保险库解锁失败，也不能仅由该结果断言模型不存在或 API 密钥无效。后续排查聚焦 Host 执行与事件处理。

已复现的根因：DSH 将 run/term/step 标识与游标直接拼接为事件编号，正式验收生成的首条事件编号长达 133 字符，超过控制面 `OpaqueIdentifier` 的 128 字符上限，导致事件验证失败及只读恢复循环。修复采用固定长度、确定性的事件编号；原始 run/term/step 字段与游标继续独立保留，不放宽协议限制。离线故障回归仅证明协议与失败处理，不代替此模型的真实云端验收。

同时保留已知供应商错误的受控分类：当底层明确报告传输、认证、限流或 HTTP 请求失败时，人工验收显示 `provider_request_failed`；不向界面发送异常原文、HTTP 正文或密钥。未知运行时错误仍不推断为密码问题。

本次补丁由下一次验收新启动的外部验证进程和 DSH 子进程加载，无需为了加载该补丁重启 GUI，也无需锁定已经解锁的保险库。保留已保存的模型配置，重新在验收密码框输入原主密码后点击“重新验收”即可。

### 2026-09-05 验收通过与会话环境接续

| 项目 | 已验证结果 |
|---|---|
| 验收编号 | `4898e6381c184ba19a84cc321617c91e` |
| 供应商 / 模型 | `deepseek-primary` / `deepseek-v4-flash-vision-exp` |
| 运行时 / 构建 | `dsh` / `dsh:model-host-v2-r1` |
| 真实端点 / 终态 | `cloud` / `completed` |
| 记录的执行时延 | 1015 ms |
| 准入复核 | 正式加载器验签通过；当前 Profile 摘要与模型匹配；`DEV_UNTRUSTED` |
| 证据到期时间 | 北京时间 2026-09-05 19:25:13 |

证据保留在用户运行目录的 `manual-runtime-verifications/4898e6381c184ba19a84cc321617c91e/`。接续时仅复制公开签名证据、公钥与清单到该运行目录根部，保留原始验收目录，不修改密码、API Key 或原有会话。界面成功提示本身不执行这一步部署。

该开发证据有一小时有效期，不代表生产信任。到期、模型配置变化或运行时构建变化后，须由用户重新通过界面验收，再复核部署新证据；不得延长旧证据时间或把历史成功当作当前准入。

会话手测需要同时启动正式 DSH Host 并开启开发证据加载。客户端重启后保险库默认锁定，用户使用原密码解锁；随后在会话中选择 **Agent-事件驱动模式（DeepSeek Harness）** 和上述模型。优先手测普通消息、同会话追问、刷新后历史恢复及取消运行。当前 DSH 模型 Host 不宣称 Tool、Skill、Workspace 执行或多 Agent 全流程已通过；这些仍是后续独立验收项。

### 历史任务暂停与本轮手测环境

用户批准“暂停历史任务，只测试新会话”后，已对确认的 **31 条 DeepSeek 历史任务**写入独立的持久化 manual hold。原任务仍保留 20 条 queued、11 条 retryable 状态，因此旧时间线可能仍显示原排队记录；实际 Worker 领取及执行入口均拒绝 held 任务，重启和普通会话恢复不会自动解除此暂停。需要恢复时必须明确解除相应 hold。

操作前后这 31 条任务完整记录的摘要一致，数据库中的 235 条任务、248 条消息、22073 条事件数量均未改变。重启后再次确认 active hold 为 31、运行中的云端任务为 0，DSH 注册为 ready。未删除或重建用户历史。

旧会话超出 IPC 大小限制的问题已改为 256 KiB 分页读取。真实 `ui-session-0` 的 15341 个 SSE frame、5189556 字节经 20 页重组，与原完整响应逐字节一致。实际客户端重启后已显示旧消息，来源恢复为 `Task 3 REST/SSE · cursor`，不再持续等待本地服务。这证明历史读取修复，不等同于已完成新会话真实模型发送、取消或整个联邦验收。

当前客户端停留在模型供应商的原保险库解锁页。请解锁后使用 **新建会话 → 单 Agent**，再选择 DSH 与已保存的验收模型；不要向被冻结的旧任务继续发送。所有新模型调用仍由用户点击发送触发。
# 2026-09-06：模式不可选的冷启动修复

## 10:39 实际客户端复验

用户已完成新验收 `e686a7c8e239488a8dbefda9eed85b00`：DSH / deepseek-primary / deepseek-v4-flash-vision-exp / cloud / completed / 1755 ms。使用当前时间的正式加载器在隔离 SQLite 验证签名、构建、能力及当前供应商/默认模型绑定通过。新证据到期时间：2026-09-06 11:07:57（北京时间）。

正常关闭旧客户端与后端后，备份运行目录旧四份公开证据至 `admission-backup.Gbxxvz`（保留原验收目录），加载完整新公开证据，manifest 最后复制。以已修复代码及明确的 DSH Host 配置重启；实际运行库记录新证据于 10:37:38 导入。

实际 GUI：用户新建“架构师 · 新任务”（`ui-group-1788662036258-2`）的运行模式已显示选中 **Agent-事件驱动模式（DeepSeek Harness） · DEV_UNTRUSTED**，模型显示 **deepseek-v4-flash-vision-exp**；发送按钮可用。不是仅用数据库 ready 推断可选。重启后 Vault 默认锁定，已切换到模型供应商并聚焦原主密码框；用户解锁后返回此新会话发送。未替用户发送模型消息，未重新发起收费验收。

历史任务复核：31 条持久化 hold 保留，运行中的 DeepSeek 历史任务为 0。以上确认模式选择问题已在实际客户端恢复；不等同于新会话端到端执行或三通道整体验收通过。

## 故障原因与修复记录

### 2026-09-06 新会话排队与执行前失败

15:49 真实会话复测：解锁后，通过 GUI 以 DSH + `deepseek-v4-flash-vision-exp` 重新提交原业务内容，新命令后缀 `ba5310a4-8c85-49e2-9065-d1695c3c862a`。Grant 已消费，流式投影到游标 197、收到 353 字正文，随后 `runtime_failed`；不是密码或模型连接失败，不能宣称修复后端到端通过。正文中断于“需要协调国资委”。只读复现确认公共文本过滤会拒绝 `/`、`国资委/集团`、`项目总监 / 业务架构师`；这与中断位置吻合，但原丢弃事件未保存，关联原因仍需精确诊断。

随后单独发起短回复探针（命令后缀 `3d061437-e5be-497b-a4f0-881daaa64d0c`），未产生新租约，进入 `runtime_unavailable` 重试，尽管持久化注册仍为 ready。已通过正式 hold 接口暂停此诊断探针，避免无人值守续跑；先前 40 条暂停记录保持不变。后续需联合核对流式公共文本投影与异常后租约可用性，不应再次要求用户重置密码，亦不应通过回退聊天模式掩盖故障。本轮未修改相关过滤或租约实现。

15:43 接续：用户验收 `1f86d94205914cb9859899305d097631` 已由正式加载器按当前时间验签通过，Profile/模型匹配，DSH 云端终态 `completed`，证据有效至北京时间 16:41:03。公开证据加载到运行目录后，已重启客户端加载 `8891851` 修复；40 条活动暂停记录不变。实际 GUI 已选中 `Agent-事件驱动模式（DeepSeek Harness） · DEV_UNTRUSTED`，模型为 `deepseek-v4-flash-vision-exp`，原业务消息恢复为未发送草稿。旧失败记录保留。当前停在重启后的 Vault 解锁界面，尚未发起修复后的新会话请求，不能将本记录当作会话端到端通过。

- 用户新命令已持久化，但最早的历史 LM Studio 重试一直抢占单 Worker。调度改为按最后更新时间轮转候选任务，并保留同会话的 enqueue 顺序约束；进程重启后仍保持公平性。
- 在原 31 条 DeepSeek 暂停记录之外，通过正式 `hold_turn` 接口暂停 9 个历史本地队头，合计 40 条活动暂停记录。历史数据未删除，同会话后续任务继续受 FIFO 约束；不会为了新测试自动恢复旧任务。
- 解除队头阻塞后，用户命令在 11:27:38（北京时间）进入执行并记录 `runtime_failed`，发生于 Provider Grant 签发前，不能视为云端 API 失败。控制面 Assignment 保存原始会话 ID，Query Envelope 却带 `conversation-session:` 前缀，触发 Host 身份校验。
- 修复 Envelope 使用原始会话 ID，命名空间只保留在上下文 Snapshot 引用；准入时提前拒绝会话 ID 不一致，避免持久化无效 Assignment。Grant Target 解析失败也关闭租约，避免遗留执行句柄。
- 新增回归先复现失败再修复；队列/准入/Grant 相关组 **113 passed**，主装配/Federated Executor/Supervisor/冷启动组 **112 passed**。这些是代码回归，不替代真实云端端到端验收。
- 原消息与失败事件保留，不改写旧冻结 Envelope 或 Assignment、不回退聊天模式。重新发送必须使用新命令身份；可沿用原消息内容，不需重新编写需求。
- 此次客户端重启仍需当前有效的正式开发证据以及用户在界面解锁 Vault。已过期证据不延长、不重签旧结果。修复后的真实发送结果另行记录；在此之前不宣称端到端通过。

用户截图中的 `catalog_unavailable` 不等于模型 API 不支持，也不代表 Vault 密码错误。正式目录在 `build_app` 时加载，早于 Supervisor 启动；新的 Registry 实例对持久化记录尚无本次进程的 advertisement，因此返回 `unavailable`。旧加载器要求记录已经 `ready`，导致重启时丢弃目录；后续 Host 握手成功也不会恢复该目录。之前只检查 SQLite 中的 `ready` 不足以证明 UI 可选择。

修复：对已明确配置的 Goose / DSH，允许身份和能力摘要匹配的冷启动记录导入**已验证且未过期**的目录；仍由实时 Admission Probe 在当前握手成功后开放选择。禁用、未配置冷记录、构建/能力漂移、过期证据仍拒绝，不自动降级为聊天模式。

验证：隔离 SQLite、测试签名证据的冷启动回归修复前 2 failed / 5 passed，修复后相关组 68 passed。这是加载链路回归，不计为真实云端模型验收。用户原验收 `4898e6381c184ba19a84cc321617c91e` 的证据已于 2026-09-05 19:25:13（北京时间）过期，未延长或重签旧结果。

下一步：在“模型供应商 → DeepSeek Harness 人工验收”输入原 Vault 密码并手动点击“开始真实验收”；成功后加载完整新公开证据并重启更新后的后端，再验证新会话下拉框和发送路径。31 条历史 DeepSeek 任务继续保持持久化暂停。**新证据加载与实际界面可选性仍待复验，不宣称端到端完成。**
